# 项目实践：清理目标工程的诊断

## 上下文

- **目标工程**：`D:\github\vscode-lua\server`（上游 master 架构，不是本仓库）
- **本仓库**：`D:\github\vscode-lua-4\server`（4.0 重构分支，长期目标是替代上游）
- 加载目标工程：
  - `test\project\external.lua` —— 只加载/索引
  - `test\project\external-diagnostic.lua` —— 加载并输出诊断报告（配合 `--test-project`）
- **当前阶段（2026-09 起）**：用本仓库的 LS 去扫目标工程，逐条清理诊断。目的有两个：
  1. 找出本仓库的**误报**并修掉（对齐本仓库自己的语义与用户预期）；
  2. 反过来检查目标工程的注解/用法是否本身有问题（注解写错也会被诊断暴露）。
- 这个实践会持续一段时间，**每次会话都应先读本文件**。

## 遇到诊断时的处理顺序（重要）

**先判断语义，再怀疑实现。不要一上来就调试 LS（tracer / coder / 类型系统）。**

1. 读代码上下文，先确认写法/注解本身是否正确：
   - 可选字段有没有写 `?`（`---@type { a: integer, b?: integer }`）；
   - `---@type` / `---@param` / `---@return` 与实际赋值、返回值是否一致；
   - 是否缺少 `---@async`（异步上下文）、`---@overload`（重载）、`---@class` 等标记；
   - 是否依赖跨文件推断才成立。
2. **不要用上游 master 做对照**（不跑 master 的二进制、不拿 master 的输出当"应该报/不该报"的判据）。
   判断只基于本仓库自己的语义、现有测试与目标工程代码本身：
   - 写法/注解本身有误 → 先改代码或注解（此时本仓库的注解语义就是判据）；
   - 只有我们报、且按本仓库自己的语义就不该报 → 才是本仓库的候选 bug，进入实现排查。
3. 确认是误报后，再按 skill 的 references 定位子系统，并补上回归测试。

### 案例：`script/config/env.lua` 的 assign-type-mismatch

- 现象：`---@type { name: string, key: string, converter: fun(value: string): any }[]` 要求每个元素都有
  `converter`，而 10 个元素里 9 个没有 → 我们报 `assign-type-mismatch`。
- 结论：**注解写错**——`converter` 应写成 `converter?`，已由维护者改正。
  （这次没有改我们的 cast 语义，差异记录在"已知未决"。）

## 常用命令（在 `server` 根目录执行）

- 全量测试：`bin\lua-language-server.exe --test`
- 单套测试：`bin\lua-language-server.exe --test feature.diagnostic.param-type-mismatch`
- 扫目标工程（外部诊断报告，输出各诊断码计数 + 样本行）：
  `bin\lua-language-server.exe --test project.external-diagnostic --test-project=d:\github\vscode-lua\server`
  - 样本行格式 `文件:行:列 | 源码行 | 消息`；多行消息会占多行，按行 grep 会被截断，必要时读原始区段
  - 输出重定向到本仓库 `tmp/` 便于比对（例如放宽/收紧前后各扫一次）
- 想把某个小写法单独拿出来复现：把它放进本仓库 `tmp/` 下的临时目录，用
  `--test project.external-diagnostic --test-project=<那个临时目录>` 扫一遍即可（不要动目标工程）。

## 约定

- **判断"该不该报"只依据本仓库自己的语义、现有测试与目标工程代码本身**：
  不跑上游 master 的二进制、不拿 master/其他实现的输出当判据（这条是硬规则，见 `AGENTS.md` §0）。
- 用户说「**继续**」时的默认动作：扫**目标工程**（不是本仓库自扫）→ 挑一个「确认是我们实现 bug
  且比较好修」的误报 → 修掉并补回归测试 → 再读**本仓库问题面板**（`vscodeOperator_readProblems`，
  `minSeverity=information`）确认警告没有新增（目标：0）。
- 本仓库自扫（`--test project.external-diagnostic --test-project=<本仓库>`）**默认不做**，
  除非明确要求；本仓库只看问题面板。
- 临时文件一律放本仓库 `tmp/`；**不要**在目标工程里留下文件。
- 删除文件用 git 命令（`git rm` / `git clean -fdx -- <路径>`），不要用 `del`。
- 目标工程的改动只允许是"修注解/修代码"这类正当修复，不做无关改动。

## 这段时间已确认并修掉的问题（本仓库侧）

- `undefined-field` 误报（目标工程 123 → 105）：**开放表的标记要穿过值合并**。
  未注解的字面量表被 `setDynamic()` 标成开放结构后，只要它后面被写过字段
  （`local t = {}; t.a = 1`），值就会被 `rt.mergeTables` 重建成**一张新表**，
  标记丢失 → `t.b` 又被报未定义字段。修法：`mergeTables` 里只要有一张开放表，
  合并结果也标成开放（`rt:mergeTables`，`script/node/runtime.lua`）。
  回归测试：`test/feature/diagnostic/undefined-field.lua`。

- `redundant-return-value` 误报（目标工程 1 → 0）：`Node.List` 展平元素时，
  内层列表（如 `setmetatable` 泛型返回的第一项）取不到值时会把元素整个丢掉（`values[i] = nil`），
  返回值个数随之被算成 0，于是「函数最多返回 0 个值」这种误报。改成取不到就退回 raw 元素
  （`values[i] = value.values[1] or raw`），个数不再凭空丢失。
- `undefined-field` 误报（目标工程 144 → 123）：**未注解的表字面量是开放结构**，任意具名字段都可能存在，
  不该报未定义字段（`local t = {}` / `local t = { foo = 1 }` 读未知字段、`t:m()` 方法调用、
  先写后读未知字段都一样）；只有 `---@type {...}` 与 `---@class` 这类**注解过的**表才是闭合结构。
  修法：`table` 字面量的 coder 里 `{key}:setDynamic()`，`Node.Table:hasDynamicKey()` 认这个标记
  （provider 已有的「开放结构」出口即生效）；视图不变（别加 `[unknownkey]: any` 字段，那会污染
  `{ insert: 1 }` 这类 view 断言）。回归测试：`test/feature/diagnostic/undefined-field.lua`。

- `undefined-doc-name` 误报（目标工程 3 处 → 0）：
  ① `cats/union.lua` 的 `parseCatDescription` 一遇到 `|`/`&` 就结束描述，于是同行描述里的
  `# Event flags (SELECT_READ | SELECT_WRITE)` 被并进 union，`SELECT_WRITE` 被当成类型名报未定义；
  现在只有**跨行续行前缀**（`---|`/`---&`，即 `|` 前有 `---` 的注释符）才把 `|`/`&` 交还给 union，
  其余一律算描述文本，且 `# desc` 取到行尾（顺带修掉描述被 `|` 截断）。
  ② 反引号里的字面名字（`catid` 的 `asCode`，如 `` `fs.copy_options.overwrite_existing` ``）
  是按文档约定写的字面量，不参与类型解析，不再报未定义。
  回归测试：`test/feature/diagnostic/undefined-doc-name.lua`。

- `missing-parameter` 误报（目标工程 4 处 → 0）：
  ① 末位实参是**多返回值调用**时按展开计数（`f(g())` 实际收到 g 的全部返回值），
  原先只数 `#args` → `converter.position(guide.rowColOf(x))` 被报「requires 2 argument(s)」；
  ② `?` 后缀在 `catarray` 上被丢掉（`coder/cat.lua` 的 catarray provider 没并 `nil`），
  于是 `---@param errs typecheck.err[]?` 的形参被当成必填 → `vm.canCastType(uri, def, ref)` 被报
  「requires 4 argument(s)」；现在 `integer[]?` 的类型是 `integer[] | nil`（顺带修掉 1 处
  param-type-mismatch）。回归测试：`test/feature/diagnostic/missing-parameter.lua`、
  `test/coder/type-annotation.lua`。

- `redundant-parameter` 误报：内联 `fun(...)` 出现在**列表项的类型位置**时，它的返回值列表会把外层列表的
  `,` 当成自己的返回值分隔符，于是后面那几项「消失」了。典型是
  `---@overload fun(f: integer|async fun(...):..., index: integer)`（`debug.getlocal`）解析成只有 1 个形参
  → 调用 `f(1, 2)` 被报「expects a maximum of 1 argument(s)」。
  修法（`parser/ast/cats/function.lua`，见「内联 fun 的返回值边界」）：
  ① 裸 `...` 返回值是 spread（原先退化成名为 `...` 的普通返回值），而 **spread 只能是最后一项** →
  列表在 spread 处结束，后面的 `,` 归外层（`...`、`:...R` 两种情况都靠这条修好，与上下文无关）；
  ② 逗号后解析不出返回值时把逗号还给外层（恢复 `lexer.ci`），不再静默吞掉分隔符。
  目标工程 `redundant-parameter` 2 处消失（686 → 684，语法错误仍为 0），
  回归测试：`test/parser/ast/cat.lua`、`test/feature/diagnostic/redundant-parameter.lua`。

- `missing-return-value` 误报：返回值计数把表达式个数当值个数（`...` 展开/不定长要跳过）
- `Table:onCanBeCast` 与 `Union:get` 互相递归导致栈溢出
- 全局变量在 flow 中的自引用读值（典型：`LOGPATH = LOGPATH and ... or ...`）
- 动态键读取（`t[k]`）的收窄泄漏：不同键共用槽位，假分支收窄成 `false | nil` 污染其他键的读取
- `await-in-sync` 两族误报：① `---@async` 写在 function 上一行且处于实参/表构造位置时没绑到字面量
  （`---@diagnostic` 这类注解此前不进注解组，导致"上一行"判定失准）；② 实参传给 `async fun()` 形参的
  函数字面量应视为异步上下文
- Lua 5.5 命名不定长 `...args`：命名表的元素类型原先恒为 `any`，现跟随 `---@param ... T` / `---@vararg T`
  （`n` 仍固定为 `integer`）；有 `---@overload` 时依旧按重载生成精确表
- 表字面量类型里的可选字段被当成必填：`cattablefield` 生成 `rt.field(key, value)` 时丢了 `?` 标记，
  于是 `{ name: string, converter?: fun(...) }[]` 这类注解在「缺失可选字段」时报 `assign-type-mismatch`
  （目标工程 `script/config/env.lua:14` 就是这条，把注解改成 `converter?` 也没用，因为标记本身丢了）。
  现在 `rt.field(key, value, true)` 会把 `nil` 并进字段值，缺失的可选字段不再报错；非可选字段缺失仍报。

- `param-type-mismatch` 一族误报（目标工程 636 → 537，p-t-m 364 → 270，need-check-nil 68 → 63）：
  **字段读取的收窄值要跟基变量的 flow 值走**。`---@type T?`（或返回 `T?` 的调用初值）的局部被
  guard 收窄成非 nil 后，读它的字段仍按注解推导：`(T?):get(field)` 把 nil 成员那份 nil 一起算了进来，
  于是 `state.ast` 被当成 `{} | nil`（`guide.eachSourceType(state.ast, ...)` 就是这一族，约 100 处）。
  修法（`script/node/tracer.lua`）：
  ① 新增 `W:deriveFieldValue(id)`：按 `parentMap` 用**基变量的 flow 值**派生字段值（基变量已被收窄）；
  ② `W:traceRef` 在派生值不含 nil 时改用它（仍带 nil 不采纳，避免把更含糊的值引进来）；
  ③ 调用收窄/向上传播这几处从栈上取字段值的地方改用 `W:getFieldNarrowValue(id)`（栈上没有时派生），
  否则 `assert(x.a)`、`if f(x.a) == 1` 这类收窄会因为栈上没有该字段的值而整个跳过。
  派生值**不写回收窄栈**：字段读取的记忆值会掩盖基变量后续的重赋值。
  回归测试：`test/node/tracer.lua`（`A | B` 注解 + flow 收窄为 `A` → `x.a` 得 `string` 而非 `string | nil`）。
  遗留：目标工程因此多出 1 处 `script/vm/node.lua:351`（`---@cast c -vm.global` 后 `removeObject(c)`）——
  把 `c.cate` 的「联合冗余 nil」去掉后，`c.cate == 'type'` 的向上传播不再排除 `vm.global` 成员。
  值本身更准，问题出在收窄传播链上；根因是**`---@cast` 在这个分支里根本没实现**（见「已知未决」）。

- `param-type-mismatch` 误报（目标工程 537 → 527，p-t-m 270 → 261，need-check-nil 63 → 62）：
  **谓词调用不该把实参收窄成含 nil 的形参变量**。`if not config.get(uri, 'k') then return end` 这类写法，
  被调函数形参可选（`---@param uri uri?`）时，调用收窄会把实参换成「形参注解变量」（view `uri | nil`），
  于是之后的读取都多出一个 nil（目标工程 `provider.lua` 的 `local uri = files.getRealUri(...)` 一族 9 处）。
  修法（`script/node/tracer.lua`）：
  ① `isNilValue` / `isDefinitelyNotNil` 先展开变量包装（新增 `resolveNarrowValue`）——
  原来的判定只看 `type`/`union`，收窄结果包成 `variable` 时根本看不到里面的 nil；
  ② 判定实参是否「确定不是 nil」改用 `argValue:simplify()`：`local uri = f()` 这种实参的值本身是
  调用的返回值（`select`），不展开就判不出非 nil，整条守卫等于没生效。
  回归测试：`test/feature/diagnostic/param-type-mismatch.lua`（形参可选 + 调用取值的局部 + guard）。

- `param-type-mismatch` / `return-type-mismatch` 误报（目标工程 527 → 514，p-t-m 261 → 250，return 23 → 21）：
  **`---@cast` 在这个分支里原本没实现**，现在按位置的收窄补上了。目标工程 75 处 `---@cast`
  （绝大多数是不带 op 的断言，如 `---@cast source parser.object`、`---@cast c vm.global`）之前完全没生效，
  只能靠前后条件的收窄链碰运气。做法：
  ① parser：`parseCatStateCastItem` 里 `local exp = self:parseCatExp()` 是**影子变量**，外层 `exp` 永远是 nil
  （`item.type` 拿不到类型）——去掉 `local`；
  ② coder：`catstatecast` 用 `coder:compileToPrelude(item.type)` 把类型值编译到**文件前导区**
  （cast 常在分支里，落在分支里的赋值可能根本没执行，而 walker 走整条 flow 都会读到它），
  再发一条 flow 指令 `{'cast', varId, op, typeKey, isOptional}`；
  ③ tracer：`W:traceCast` 对**当时的** flow 值应用——不带 op = 断言（直接用注解类型）、
  `+T` = 并入、`-T` = **按同名去掉成员**（不能按 `canCast`，那会把 T 的子类连坐去掉：
  `---@cast p -fs.path` 时 `fs.dummy` 继承 `fs.path`，应当保留）、`?`/`-?` = 并入/去掉 nil。
  形参在函数 flow 里没有 `var`（赋值记在闭包外层），此时只支持不带 op 的断言。
  回归测试：`test/feature/diagnostic/param-type-mismatch.lua`（断言 + `-T` 保留子类两条）。
  else 分支的 cast 归属问题已修（见「已知未决」），`vm/node.lua:351` 现在卡在**值被物化成表**上。

- `undefined-field` 误报（目标工程 514 → 491，undefined-field 105 → 82）：
  两个来源一起修（`script/node/variable.lua`、`script/node/table.lua`）：
  ① `Variable:get` 的循环守卫（`_getVisiting`）把「自引用/循环求值」当成 `exists = false`，
  消费方（undefined-field）于是认定字段**不存在**；改成 `ANY, true`（值不可判定，不代表字段不存在）。
  目标工程 `parser/compile.lua` 里 `local loc = locals[n]` 之后读 `loc.const` 一族（20+ 处）就是它。
  ② `Table:get` 的**字面量键**分支只跳过 `unknownkey` 字段，而 `X[表达式] = v` 这种动态键写入在运行期
  会把键**求值**（`#list + 1` → `value 1`、`op.add<...>`），字面量读取因此命中这些记录 →
  字面量读取只匹配字面量/类型键字段。
  遗留（下轮）：`Table:get` 的 `valueMap` 分支返回的是**字段记录**而不是字段值，definition 等功能
  依赖这一点（去掉就会让 `t.x` 找不到定义），所以这次没动；纯值消费方（tracer 的字段派生等）
  拿到的仍是 field 节点。要把这条理顺，得先让 definition 一侧改走 `fieldMap`/`valueMap`。
  这两个改动没找到能钉住的最小用例（同形状在测试 harness 里不报，目标工程那份依赖上下文），
  回归以目标工程扫描为准（`parser/compile.lua` 一族）。

- `undefined-field` 误报（目标工程 457 → 449，undefined-field 69 → 62、p-t-m 233 → 232）：
  **读取本身创建的子变量不是「字段写记录」**。`local t = {}; local w = t[1]` 这种开放表上的字面量键读取，
  coder 会生成 `r[t]:getChild(1):shadow()`，于是 `t.childs[1]` 出现一个只有 `childs`、没有 `assigns` 的
  子变量；`Variable.childsValue` 原先按 `var.assigns or var.childs` 判定，把它当成写记录，
  `t` 的值里就多出一个「值=该变量自己」的字段记录，读取时又经 `parentFieldValue`/`valueMap`
  绕回同一条记录 → 派生值既是字段记录又是自引用变量 → 下游（`w:match`）读不到字段就报未定义。
  修法（`script/node/variable.lua`）：`childsValue` 改为「该子变量自身或其后代有赋值才算写记录」
  （`hasAssignedField`，带 visited 防自引用环）。嵌套写入的中间层（`t.a.b = 1` 的 `t.a`）因后代有赋值仍然保留；
  读取创建的子变量不再进类表，开放表的任意键读取回落成不可判定值（ANY）。
  回归测试：`test/feature/diagnostic/undefined-field.lua`（读取不报 + 写入/嵌套写入仍是已知值两条防假绿）。

- `param-type-mismatch` 一族误报（目标工程 449 → 429，p-t-m 232 → 217、undefined-field 62 → 59、
  need-check-nil 58 → 57、return 21 → 20）：**泛型 for 的第一个控制变量在循环体内恒非 nil**。
  迭代器返回 `T?`（如目标工程 `files.eachFile` 的 `---@return fun(): uri?`）时，`for uri in files.eachFile(...)`
  的循环体内 `uri` 仍带 nil，于是 `furi.decode(uri)` 一族被报「Cannot assign `uri | nil` to parameter `uri`」。
  做法（新指令，落在新的 tracer 路径上，没动 flow/branch）：
  ① `script/vm/coder/tracer.lua` 新增 `appendNonNil` / `appendNonNilEnd`（发 `{'nonnil', id, alias}` /
     `{'unnonnil', id}`）；
  ② `script/vm/coder/block.lua` 的 `for` provider：`in` 型循环在控制变量赋值之后、循环体之前发 nonnil，
     循环体之后再发 unnonnil（第一个控制变量才收窄，第二个（`for k, v` 的 v）可以是 nil，不收窄）；
  ③ `script/node/tracer.lua` 的 `traceUnit` 处理这两条：nonnil 走 `traceByValue(ref, rt.NIL, revert=true)`
     （只去 nil，**不**按 truthy 去 false——`for x in f()` 里 x 可以是 false），并记下原值；unnonnil 还原。
  用 `rt.NIL` 而不是 `rt.TRUTHY` 是关键：`false` 不会终止循环。
  回归测试：`test/node/tracer.lua`（nonnil/unnonnil 前后两次读取）、
  `test/feature/diagnostic/param-type-mismatch.lua`（第一个变量不收窄后报错、第二个变量仍报错）。

- `need-check-nil` / `undefined-field` 一族误报（目标工程 418 → 400：need-check-nil −7、undefined-field −11，
  文件数 71 → 69）。两处独立成因，都在本仓库侧：
  ① **间接窄化不能写「无信息」的结果**（`script/node/tracer.lua` 的 `W:traceTruthy`）：
     `local t, k, isarray, lastpath = query(...)` 这类**同一次调用的多个返回值**互相 link，
     对其中一个做真值判断时，会把其他返回值也过一遍 `narrowCall{mode='match'}`。
     多个返回值彼此独立时（`---@return table?, string?, boolean?, string?`）该 narrow 会退化成
     「等于原值」甚至 `never`，写回槽位就**覆盖掉已有的守卫收窄**（`if not t then return end` 收窄出的
     `table` 被 `table | nil` 顶掉），字段读的派生值因此掺回 nil（`t.v` → `any | nil`），
     下游 `t.v[k]` 报 need-check-nil（目标工程 `json-edit.lua:628,629,633,662,666,705` 即此族，
     探针 `tmp/probe-guard7`）。
     判据：拿「实际写进 current 的那一侧」与当前值比较，`otherValue:canCast(结果)` 成立（老值已满足新值，
     等于没信息）或结果为 unknown 时不写。**注意**：完全关掉这段 link 传播还会**暴露**另一族
     （`tmp/probe-typeformat`：未知模块返回值的字段读被 `never` 掩盖），所以判据要收窄到「无信息」，
     不能整体禁用。
  ② **`undefined-field` 要按「有效值」判 nil/never**（`script/feature/diagnostic/providers/undefined-field.lua`）：
     原先只看 `node.kind == 'type'`，而 `local a, b = f()` 里第 2 个返回值是 `select` 包裹的 `never`
     （载体 kind 是 `select`）→ 漏判 → 报未定义字段。改成先 `node:simplify()` 再判 nil/never
     （函数没有对应返回值、跨文件未知模块等情形，字段存在性本就不可判定）。
  回归测试：`test/feature/diagnostic/need-check-nil.lua`（多返回值 + 守卫 + 动态键字段读，
  关掉判据会失败）、`test/feature/diagnostic/undefined-field.lua`（多返回值/未知模块 + 真未知字段对照组）。

- **`C stack overflow`（编辑器实测报错，和上面那轮改动无关）**：`log/service.log` 里
  `[error][script\luals.lua:55]<master>: C stack overflow`，出现在**编辑器**启动后约 20 秒的
  **全工作区诊断**（`feature/diagnostic/scope.lua:54`）里，紧跟目标工程 `script/parser/compile.lua`
  的 publishDiagnostics；命令行 `--test project.external-diagnostic` 扫同一个工程**不触发**
  （并发/重入深度不同，命令行侧更浅）。
  栈（`skipping 1996 levels` 那段的端点）：
  `undefined-field.lua:50 getNode` → `variable.lua:943`（`Variable.value` 里 `tracer:trace()`）
  → `tracer.lua:303 traceBlock` → … → `intersection.lua` 的 `values`(47) / `value`(141) / `hasGeneric`
  三者互递归；再往后（param-type-mismatch 那条链）也是同一族。
  **根因**：`buildValues` 里遇到 union raw 就 `rt.intersection(parts)` / `rt.union(newValues)`
  **造新对象**（`script/node/intersection.lua:87-97`），每层对象身份都不同，于是
  `self.value = rt.NEVER`（`Intersection.value` 的在算标记）和 `Field.hasGeneric = false`
  这类「同对象在算」标记都挡不住 —— 类型图无限增长。
  **修法（本轮）**：给 `Intersection` 的 `values` / `hasGeneric` 加**深度护栏**：
  `DEPTH_LIMIT = 64`，计数挂在当前 `Task` 上（`ls.util.weakKTable` 弱键，避免跨请求泄漏/常驻），
  无 Task 时退回模块级计数；超限分别返回 `{ rt.ANY }` / `false`，并节流（5s）`log.error` 一条带栈的信息
  （栈里能看到真实环路，便于后续查根因）。选 64 的理由：实测溢出的环路约 1000 层级别，
  护栏在 64 层就收敛，离 C 调用上限还有几十倍余量；而合法类型嵌套远不到 64。
  **验证方式**：编辑器里 `lua.stopServer` + `lua.startServer` 重启 LS 看 `log/service.log`
  （改前 15:18 / 15:32 / 15:37 三次会话各 `C stack overflow`；改后 15:55 会话 0 次 + 3 条护栏日志）；
  `--test node` / `--test feature` 全绿；目标工程扫描仍 401 且命令行侧不触发护栏（结果无变化）。
  **排除了本轮改动**：把 `traceTruthy` 的 link 传播整块 `if false`（回到改动前行为）重启 LS 仍崩，
  且崩的帧是原始行号 → 与本轮 tracer/provider 改动无关。
  **仍未解（已根治，2026-09-18 第二轮）**：无限增长的真身是 `buildValues` 的 union 分布
  遇循环回边/泛型解析造出的**无限新树**（每层新对象，非同一对象环），深度爬升几乎全在
  `hasGeneric` 帧。根治：`values`/`hasGeneric` 两 getter 各挂结构化签名在算集
  （变量取 key 名、call 取 head/参数数、其余取身份），命中即收敛（values→`{}` 丢弃递归分支
  ≈ 最小不动点，hasGeneric→`false`），深度护栏保留兜底。验证：编辑器 0 次 overflow、
  环在深度 2~6 截断；`--test node`/`--test feature` 全绿；目标扫描 401 不变。
  回归：`test/node/intersection.lua`。更深根因：tracer 求值非只读（`setCurrentValue` 在求值中途
  flush 缓存导致反复重算新对象），属 tracer 层独立任务；结构化签名的已知边界是对
  「同名变量/同形调用在一次求值内合法嵌套」会误判为环（软化不崩溃）。
  `.github/skills/luals-server-dev/references/known-issues.md` 已于 2026-09-18 清空；
  清空前入库的旧条目（meta 忽略项、类型求值无深度守卫、2026-08-28 truthy/falsy 环）
  见 `git log -p -- .github/skills/luals-server-dev/references/known-issues.md`。
  同轮（2026-09-18）写入该文件的两条护栏/根治条目未入库即被清空，其要点即本文以上内容。

## 2026-09-18（第三轮）：meta 括号类型语法 + 扫描内存爆炸

**背景**：编辑器在目标工程 `meta/template/basic.lua:292` 报 `miss-symbol`（`Missed symbol \`)\`.`）。

- **语法已修（提交 `13f4e624e`）**：`(fun(t):((fun(t,k,v):any,any),any,any))|nil` 现在可解析。
  改法是 `script/parser/ast/cats/exp.lua` 的 `parseCatParen` 在解析内部表达式时清空
  `catInListValue`——括号自成一层，括号内联 `fun` 的无括号返回值独占 `,`，不再让给外层列表。
  结果：外层 `fun(t)` 3 个返回值（首个是内层带 2 个返回值的括号分组）。回归在
  `test/parser/ast/cat.lua`（同时钉住 `table|fun(t,k,v)` 的无返回值写法）。
  「参数不写类型视为 any」原本就成立：`script/vm/coder/cat.lua:496` 对无 `value` 的形参编译 `rt.ANY`。
- **仍缺**：`meta/template/{io,os,debug}.lua` 的 `---|>"r"` / `---|+"n"` 报 `miss-cat-name`（未实现）。
- **内存爆炸定位（未解决，已记入 known-issues）**：命令行扫整个目标工程 `meta/`（968 文件，
  其中 191 个在 `meta/default utf8/`）把机器内存吃光。根因是单文件
  `meta/default utf8/CS.lua`（659KB，Unity+NGUI 绑定）在**加载/索引阶段**无界增长：
  前缀 250 行 82MB → 268 行 126MB → 269 行 651MB → 600 行 >5GB。不是死循环，是跨类传递解析的
  规模效应（删掉该类 7 个 `UIScrollView.*` 自引用字段后 619MB → 225MB；手工合成不复现）。
  注意 `test.lua` 的内存护栏默认上限 10GB，太高；批量扫描要显式 `--mem-limit=2`，
  且**不要并发跑多个扫描**（本次就是并发 + 10GB 上限把机器吃光）。
  测量脚本落在 `tmp/`（`scan-guard.ps1` 看门狗、`prefix-sweep.ps1`、`variant-scan.ps1`、`cs-shrink.ps1`），
  工作流已写进 skill 的 `references/workflow-and-style.md`。
- **旁证**：目标工程正常扫描（456 文件，meta 被其 `.luarc` 忽略）仍是语义诊断 401 处 / 69 文件，
  与上一轮一致。

## 2026-09-20：parser 注释重复解析（内存爆炸真因）+ 内存护栏覆盖 worker

**背景**：上一轮把「扫 meta 目录吃满内存」定位到 `meta/default utf8/CS.lua` 的加载阶段，本轮继续挖根因。

- **真因（已修）**：不是 node/tracer 的类型求值，而是 **parser 里同一位置的注释被反复解析**。
  解析注解时 `parseCatUnion` 等会 `skipSpace` 跨行看到后面的注释 → `skipComment` 重新解析它并再次
  塞进 `curBlock.delayComments`；每条语句后又跑一次 `parseDelayedComments`，队列反复膨胀，
  `ast.comments` 从应有的 ~239 涨到 **545519**（同一条注释被解析 361 次）。
  触发形状需要 `---@source` 这类**未知注解** + 连续 `--` 注释块 + 类/字段注解 + 语句交替。
- **修法**：`script/parser/ast/ast.lua` 的 `skipComment` 按注释起始位置去重（`self.parsedComments`），
  重复遇到时短注释直接 `moveTo(finish + 1)`（与 `parseShortComment` 落点等价）跳过。
- **实测**：该文件解析 0.82s / +17.6MB（修复前 >10GB、269 行前缀 1.87s/651MB）；
  `meta/default utf8` 整目录 191 文件扫描：修复前 9s 内被 3GB 护栏杀掉 → 修复后 98s 完成、峰值 1745MB。
  回归在 `test/parser/ast/cat.lua`（注释数断言）。排除项：目标工程 456 文件扫描仍是 401 处诊断。
- **内存护栏**（`script/tools/mem-guard.lua`，见 skill）：钩子只对设置它的线程生效，所以
  `coroutine.create/wrap` 会被包装以把钩子挂到新协程；`bee.thread` 工作线程是独立 Lua 状态，
  由 `script/async/worker.lua` 在测试模式把 `memLimit` 传进 worker 选项、`worker-init.lua` 各自装一份。
  实测 10 个 worker 全部 `enabled=true`；`--mem-limit=0.5` 扫 600 行前缀时在 master 触发并退出。
  另外 `test.lua` 的 bootstrap 已包 `xpcall`，避免入口早期抛错导致 `eventLoop.stop()` 不执行而挂住进程。
- **新待办**：批量扫描时 worker 编译 `UnityEngine.lua` 会抛 `Source is nil`（单独扫不复现），
  该文件因此没被索引，已记入 known-issues。

## 2026-09-20（第二轮）：残缺下标注解导致整文件不被索引

- **现象**：扫 `meta/default utf8`（191 文件）时 worker 报 `Source is nil`，该文件（`UnityEngine.lua`）
  完全没被索引；master 侧表现为 `attempt to index a nil value (local 'result')`（`coder:makeFromFile`）。
- **机制**：文件里有 C# 生成工具写出的 `---@param results float[*,*]`（7 处）。`parseCatIndex` 里
  `index.index = parseCatExp(true)` 解析 `*` 失败（只记 MISS_CAT_NAME），却仍产出 `index = nil` 的
  `catindex` 节点 → coder 的 `catindex` provider `compile(source.index)` 抛 `Source is nil`
  → 整个 makeCode 请求失败 → 该文件零节点（静默失效）。
- **修法**：`parseCatIndex` 先解析下标、成功后才创建节点；下标解析失败时按**数组**处理
  （产出 `CatArray` = `{[integer]: T}`，`float[*,*]` 等价于 `float[]`），错误照常上报。
  数组语义按约定统一为「默认无 nil 元素」：`T[]` 取值得到 `T`，只有显式 `T?[]` 才需要 nil 检查
  （已写进 skill 的 `architecture.md`「类型语义约定」）。
- **验证**：整目录扫描不再出现 worker 编译失败；新增回归 `test/coder/type-annotation.lua`
  （断言 `X` 为 `fun(results: float)`）与 `test/parser/ast/cat.lua`（断言无 `catindex` 节点），
  两者在还原修复后都会失败（coder 侧正是 `Source is nil`）。
- **另**：目标工程扫描仍为 401 处 / 69 文件；`pub.lua` 那批 `need-check-nil` 属目标工程自身
  `m.publicQueue = {}` / `m.privateQueues = {}` 缺注解（`queue[i]` 于是为可选），不是我们的误报。

## 2026-09-20（第三轮）：`Lua.diagnostics.ignoredFiles` / `libraryFiles` 落地

**问题**：编辑器里 `meta/template/basic.lua` 报 `duplicate-doc-param`——用户问「这个文件怎么没有被默认忽略掉」。

- **原因**：目标工程 `.luarc.json` 其实已经写了 `workspace.ignoreDir = ['/meta']` 和
  `diagnostics.ignoredFiles = 'Disable'`，但本仓库的 `ls.feature.diagnostic` 只读了
  `Lua.diagnostics.enable` / `enableScheme` / `disable`，**没有实现文件级开关**
  （`ignoredFiles` / `libraryFiles` 两个配置项在 `script/config/template.lua` 里有，却没有消费者），
  于是被 ignoreDir 命中的文件照样出诊断。
- **修法**：`ls.feature.diagnostic` 入口按文件过滤——`scope:findRoot(uri).kind == 'library'`
  走 `libraryFiles`，`scope:isIgnored(uri)` 走 `ignoredFiles`；取值 `Enable` 总是诊断、
  `Opened`（默认）仅在 `document.file:isOpenedByClient()` 时诊断、`Disable` 一律不诊断。
  `script/scope/scope.lua` 顺带抽出 `Scope:findRoot(uri)`（`isIgnored` 复用）。
- **验证**：`test/feature/diagnostic/config.lua` 新增用例（stub root glob 模拟被忽略文件 +
  stub root `kind='library'`），还原实现后该用例报 `expected 0 diagnostics, actual 2`；
  `--test feature/node/parser/coder/tools` 全绿；目标工程扫描仍 401 处 / 69 文件。

- **目标工程修正（2026-09-20）**：`script/core/code-lens.lua` 面板 3 条 `param-type-mismatch`
  （62 / 70 / 108）属注解写宽，不是我们误报：`codeLens.resolving.source` 标成可选（但唯一构造点
  `collectReferences` 总是给值）、`codeLens.state` 未声明为必需（`init` 里判空后即认为存在，
  `collectReferences` / `resolveReference` 直接用）。改法：`source` 改必填、`codeLens` 增
  `---@field state parser.state`，`init` 改成先取局部 `state`、判空后再 `self.state = state`。
  复扫目标工程：401 → **398**（该文件 0 条），编辑器面板同步为 0。

## 2026-09-20（第四轮）：显式标注上的「允许逆变」

- **背景**：`script/core/color.lua:10` 报 `assign-type-mismatch`（`parser.object | any` → `string`）。
  类型来源是目标工程自己的注解 `script/parser/guide.lua:80` 的 `---@field [integer] parser.object|any`
  ——即 `source[1]` 本身就是 `parser.object | any`。
- **结论**：按用户口径「手动标注类型时允许逆变」：`X | any` 等价于 `any`，可赋给任何显式标注的类型。
  实现放在 `assign-type-mismatch` provider 内（`hasAnyMember`，含 `actual:simplify()` 后的联合体），
  **不动** `Node.Union:onCanCast`——A/B 实测全局放宽会把目标工程诊断从 398 推到 415
  （+26 处新误报，多为 `any | nil` 之类），因为 `canCast` 还被收窄与表结构兼容复用。
- **验证**：目标工程 398 → **396**（`color.lua:10` 与 `sign.lua:104` 两处消失，新增 0 处）；
  编辑器 `lua.stopServer` + `lua.startServer` 后面板该文件归零；`--test feature/node/parser/coder/tools` 全绿。
  另外给诊断 harness 的断言消息补了实际诊断的 message（`test/feature/diagnostic/init.lua`），
  失败时不再只能看到 code 列表。

## 2026-09-20（第五轮）：未知调用的多返回值不该给 never

- **问题**：`script/cli/doc/init.lua:167` 的 `local ok, outPaths, err = dirty_export.serializeAndExport(...)`
  （`dirty_export` 是 `---@return table`，字段为 any）里 `ok` 是 `any`，但 `outPaths` / `err` 是 `never`。
  期望三个位置都是 `any`。
- **根因**：`matchedFuncs` 为空时 `Node.FCall:returns` 返回单个 `any`；而 `Node.FCall:select` 直接委托给
  `Node:select` 基类，基类只认位置 1（`key == 1` 返回自身），其余返回 `never`。
- **修法**：`Node.FCall:select` 在 `#self.matchedFuncs == 0` 时，任意位置都返回 `returns`（即 any）；
  已知函数仍走 `returns:select(key)`。不动 `Node:select` 基类（`local a, b = 1` 那类单值赋值仍在议，
  现在第二位给的是 `never` 而不是 `nil`，属独立小问题）。
- **验证**：`test/coder/multi-return.lua`（any 调用 + `---@return table` 模块字段调用，两个用例断言
  三位都是 `any`；还原修复后报 `expected: any, actual: never`）；编辑器重启 LS 后 hover 出
  `local outPaths: any` / `local err: any`；目标工程扫描仍 396 处 / 67 文件（分项一致，无副作用）。

## 2026-09-20（第六轮）：右值不足时多余变量应为 nil

- **问题**：`local a, b = 1` 里 `b` 读出来是 `any`（`local x, y, z = f()` 的 any 调用已在上一轮修好，
  这轮是「右值个数不足」的单值赋值）。
- **根因**：`script/vm/coder/state.lua` 的 `localdef` provider 对没有对应右值的变量
  `coder:compileAssign(var, i)`（不给值）→ 该变量没有任何赋值，读取时落到猜测值 `any`。
  而 `assign` provider（`a, b = 1`）那边本来就会 `compileAssign(exp, i, 'rt.NIL', false)`。
- **修法**：`localdef` 在有右值、但变量数多于右值数时，给多余变量赋 `rt.NIL`
  （`local a, b = 1` → `b = nil`；`local a, b = f()` 走解析器造的 `Select`，不受影响）。
  **`local x`（完全没有初始值）保持旧行为**：给它赋 nil 会牵动「nil 变量上的字段写」推断
  （`local mt; mt.__index = mt; function mt:m() end` 的 method 解析会挂，`test/feature/definition/special.lua`
  与 `test/coder/flow.lua` 都依赖旧行为），属另一处待修语义。
- **验证**：`test/coder/multi-return.lua` 新增两个用例（`local a, b = 1` → `1` / `nil`；
  `local a, b, c = 1, 'x'` → `1` / `'x'` / `nil`）；`--test coder/feature/node/parser/tools` 全绿；
  目标工程扫描仍 396 处 / 67 文件（分项一致，无副作用）。

## 自扫本仓库（2026-09）

**「处理本仓库诊断」= 用 MCP 工具读本仓库的问题面板内容**：`vscodeOperator_readProblems`
（`workspacePath` 传本仓库路径；`minSeverity=information`，只看 error / warning / information，
**hint 一概不管**），以面板里实际显示的条目为准，
注意工作区里同时开着目标工程，面板里会混进目标工程的条目 —— 用
`pathGlob = 'd:/github/vscode-lua-4/server/**'` 过滤出本仓库的。
逐条判断「代码/注解写错」还是「LS 误报」；下面这条 `--test` 扫描只作为批量统计/回归对照。

注意：本仓库的问题面板由**扩展自带的 LS**（可能与当前工作区源码版本不同）产生，
其规则集未必等于本仓库 `script/feature/diagnostic/` 里的那套，
所以面板里可能出现本仓库 LS 现在不报、或反之的条目。**以面板为准**（用户就是这么定义的），
判断某条该不该报时按本仓库自己的语义来定，不要拿别的实现当判据。

自扫命令（诊断报告）：

```
bin\lua-language-server.exe --test project.external-diagnostic --test-project=d:\github\vscode-lua-4\server
```

- 首次可用时总数 3847，现已降到 1340（param-type-mismatch 666 / undefined-field 397 /
  undefined-doc-name 103 / return-type-mismatch 73 / need-check-nil 59 / assign-type-mismatch 22）。
- 关键修复（都是本仓库自身缺能力或缺注解，不是代码写错）：
  - `Variable:onView` 缺失：诊断消息里 view 到变量节点会抛 `Cannot view node of kind variable`
    （整轮扫描中断，典型是 `return s` 的 return-type-mismatch）。
  - `undefined-doc-name` 判定只按 `type` 节点处理，遇到 `falsy` / `truly`（运行时是 union）直接抛错。
  - `Union:onCanBeCast` 会把 `type` 节点经过 `.value` 退化成「继承链合并表」，表与类之间不可转，
    于是「子类实例传给可选参数（`Node?`）」误报（`Node.Value` / `Node.Variable` 那两族，约 460 处）。
  - `test.lua` 的 `---@class Test` 缺 `---@field scope Scope`：`test.scope.rt` 于是落成一坨裸字段表，
    `rt.value` / `rt.field` / `rt:reset()` 等全部按 undefined-field 报（约 2500 处）。
- 剩余大头：`Class 'X'` / `New 'X'` 的实参字面量被解析成类 X（本应是字面量 `'X'`），
  cast 到形参时误报 param-type-mismatch（384 处，占 param-type-mismatch 一半以上）。

### 问题面板已清条目

- `script/node/tracer.lua` seed 分支：`self.map`（`table<string, Node.Variable>`）取出的值
  直接 `---@cast ... Node.Tracer` 是非法转换（Variable 与 Tracer 无继承关系）。
  改为先经 `---@type table<string, Node>` 的视图取一次，再 cast 到 `Node.Tracer`。
- `script/node/variable.lua`：`setCurrentValue` 的形参放宽为 `Node?`
  （tracer 在查不到值时写入 nil 表示「清除收窄值」，同时触发 flushCache）。


## 设计结论备忘

- LuaCats 的 `...X` 与 Lua 5.5 的 `...name` 是**两层**含义，保持现状、不做统一：
  - LuaCats（注解层）：`... X` = 不定长元素类型；`...X` = 隐式泛型 pack 绑定（`cats/param.lua`）；
    `---@return ...R` = 解包多值。X 是**类型名**（泛型），描述的是"值"，本身不产生表。
  - Lua 5.5（语法层）：`function f(...args)` 里 `args` 是**变量**，绑定一张 pack 表（整数键 + `n`），
    由 coder 自动补 `{ n: integer }`（`script/vm/coder/function.lua`）。
- "返回一张带 `n` 的表"（如 `table.pack`）用表类型表达即可：`T & { n: integer }`
  （`test/node/pack.lua` 已钉住 `intersection{tuple, {n}}` 的行为）。因此 `table.pack` 的元声明无需改动。
- **内联 `fun` 的返回值边界**（`parser/ast/cats/function.lua`）：默认把 `,` 让给上一层，**自己要多返回值就加括号**。
  1. **spread 只能是最后一项**：裸 `...`（原先会退化成名为 `...` 的普通返回值）与 `:...R` 都让返回值列表就地结束，
     其后的 `,` 归外层。语义上 `return ...` 之后不可能再有返回值，这条不需要上下文判断。
  2. **列表项的类型值里，无括号返回值列表只取一项**：形参/返回值/表字段的类型值走 `parseCatListValue`
     （`Ast.catInListValue`）→ `parseCatReturnList(noComma)`；`fun(): (a, b)` 这类**带括号**的仍然是贪婪的
     （括号即显式终点，这就是「要多返回值就加括号」）。
  3. **逗号还给外层**：`, ` 之后解析不出返回值时恢复 `lexer.ci`，不静默吞掉分隔符。
  不受影响的既有写法：`---@type fun(): boolean, number` 保持贪婪（`---@type` 的项不走 `parseCatListValue`，
  由 `test/coder/metatable.lua` 钉住）；多行续行 `---@type fun<...>(a: T1, ...: T2)` + `---, desc: string`
  也不受影响（顶层 fun 不在列表项位置，由 `test/parser/ast/cat.lua` 钉住；具名返回值在 `fun` 返回列表里合法）。

## 已知未决

- **`for` 控制变量的非 nil 收窄带来 1 处新报**（目标工程 `script/library.lua:323`）：
  `local f, err = load(...)` + `if not f then return nil end` + `xpcall(f, ...)` 报
  「Cannot assign `function | nil` to parameter `async fun()`」——`f` 的守卫收窄在这个文件里失效了，
  而该函数里唯一的 `for` 在**后面**（`for k, v in pairs(env)`）。探针（单独复刻这个函数、`tmp/probe-for3`）
  不复现，怀疑是**执行顺序/缓存**层面的敏感（walker 在同一次 flow 里对同一 stack 的写入顺序变化，
  使某处取值退化成 PROVISIONAL → 回退到注解值）。下轮：在目标文件上下文里定位（先看 param-type-mismatch
  provider 对实参取值的来源：是读节点快照还是 `Variable.value` 的实时兜底）。
- **开放表读任意键仍不产生「不可判定」值（已试过，本轮回滚）**：把 `Table:get` 的 miss 在
  `self.dynamic` 时改成 `ANY, true`，目标工程能从 449 直接降到 397（p-t-m −21、assign −18、need-check-nil −8），
  但会**压过更精确的来源**：`t = setmetatable({}, {__index = function() return 42 end})` 的 `t.anything`
  本来由 `T & {[any]: V}` 推出 `42`，加了 ANY 后变成 ANY（`test/feature/diagnostic/undefined-field.lua`
  的假绿断言立刻挂）。根因是 `Union:get` 的 `v | ANY = ANY`、`Intersection:get` 的 `result & partValue`
  都让 ANY 支配。要拿这 −52 得先定「开放表的不可判定值如何参与组合」（不能支配、只能在整条链都取不到值时兜底），
  或是只在表达式键（`unknownkey`）路径上兜底——后者试过（`typeName == 'unknownkey' and self.dynamic`），
  目标 446 但 `script/pub/pub.lua:149` 多 1 条，且 `Chunk[#Chunk]` 一族只修掉一部分（值不是从 `Table:get` 取的）。
  参考：`queue[i].x`、`m.taskMap[id].name`、`Chunk[#Chunk].type` 都属这一族。
- **闭包里守卫收窄外层变量（upvalue）不生效**（目标工程 429 → 428，need-check-nil 57 → 56）：
  ```lua
  ---@type timer?
  local delayTimer
  local function f()
      if delayTimer then delayTimer:restart() end   -- 曾误报 need-check-nil
  end
  ```
  机制：闭包里的 tracer 是新的一层，`getValue(id)` → `getUpvalue(id)` 只在「外层创建点快照值已排除 nil」时才采纳
  （带 nil 的 flow 值被故意拒，含糊值用在闭包里会引入误报）→ 收窄没有基值 → `traceByValue` 直接 return；
  读值退回注解 `timer?` → 报 need-check-nil。
  修法（`script/node/tracer.lua`，落在新的收窄路径上）：`traceByValue` 在基值为空时退回**节点注解值**
  （`getExpectValue()`）作为收窄基值；注解值本身不会比读值更含糊，所以写进分支收窄栈是净收益。
  **`any`/`unknown` 必须排除**：对 `any` 收窄得到的是 `truthy`/`falsy` 标记，写进栈会盖住读值——实测不排除时
  目标从 429 涨到 456（修 2 处、新增 29 处，典型表现 `source.declare`「字段突然不存在」、实参变成 `false | nil`）。
  回归测试：`test/feature/diagnostic/need-check-nil.lua`（`if x then x:m()` / `if not x then return end; x:m()` /
  无注解变量不做收窄三条）。复现探针：`tmp/probe-upval`（3 处 → 0）、`tmp/probe-upval4`（各类守卫形状 0 报）。
- **`parentMap` 收窄回传链把垃圾值写回含糊基变量**（目标工程 428 → 418，undefined-field 59 → 53、
  need-check-nil 56 → 52，无新增）：
  `if src.type == 'getfield' and src.field then ... elseif src.type == 'getmethod' ...` 这类**字段条件**收窄后，
  `traceByValue` 会沿 `parentMap` 一路把结果回传给基变量（`narrowByField`）。基值为 `any`/`unknown`（或
  含 any 的 union / `select` 这类含糊值）时，回传的只是垃圾值（成员被全部过滤成 `never`/`false | nil`），
  而**下一个 `elseif` 分支的基线就是上一分支的 `otherSide`**，于是 `src` 被这个垃圾值盖住 →
  `src.type`「未定义字段」+ `src` need-check-nil（`core/diagnostics/undefined-field.lua:47`、
  `inject-field.lua:103`、`document-symbol.lua:72/75`、`jump-source.lua:42`、`provider/diagnostic.lua:349` 共 10 处）。
  修法：回传前看 `pvalue:finalValue()`，`any`/`unknown` 就不回传（也不继续往上回传）。
  与上一条的关系：入口的 `any` 拦截（不对含糊值做收窄）+ 这里的回传拦截，缺一条都会让目标涨 18~28 处。
  回归测试：`test/feature/diagnostic/undefined-field.lua`（含糊基值的 `if/elseif` 链不报 + 有类型支撑的
  union 仍按字段收窄、`x.bv` 仍报 undefined-field 两条）。
- **守卫收窄后字段读取仍按注解算 nil**（本轮勘查，未落地；目标工程里是大簇）：
  ```lua
  ---@return table?, string?, boolean?, string?
  local function query(a, b) end

  local function f(a, b)
      local t, k, isarray, lastpath = query(a, b)
      if not t then error(k) return end
      if isarray then
          return t.v[k]        -- 误报 need-check-nil
      end
  end
  ```
  复现：`tmp/probe-guard3`（3 处）；`tmp/probe-guard2`（同形状、少两个局部、无中间 if）0 处。
  定位手段（临时插桩，已移除）：① `Coder:finishTracer` 里按 id dump flow；② need-check-nil provider 里打印
  `vfile:getNode(var)` 的 kind/view 与 tracer/cv/sv 标志；③ `W:traceTruthy`/`W:traceByValue`/`W:traceVar` 打印收窄过程。
  结论：**收窄本身是对的**——flow 里守卫是 `{'return', {'condition', {'not', {'ref','t@7:11',...}}}, ...}`，
  `traceByValue` 算出 `narrowed=table / otherSide=nil`（revert 后 otherSide 即非 nil 部分），
  `traceIf` 把 terminated 分支的 otherSide 写回外层 stack（`W:getValue('t@7:11')` 确实是 `table`）。
  问题在**读值路径**：`t.v[k]` 的基变量 `t` 在 flow 里没有独立 ref（只有字段 id `t@7:11.v[unknown]`），
  于是基变量读值只能落到 `Variable.value` → `getExpectValue()` = 注解 `table | nil`；
  字段 `t.v` 同理走 `parentExpectValue` → `(table | nil):get('v')` = `any | nil`。
  provider 取的是 `vfile:getNode(var)`（= `map[key].value`，不触发 walker）→ 拿到注解值 → `mayBeNil` 命中 → 报。
  下轮修法（二选一）：① 让「基变量/字段的节点值」在基变量已被 flow 收窄时按 flow 值派生
  （把 `tracer.deriveFieldValue` 的规则用到 provider 走的 `Variable:getExpect`/`getChild`/`childsValue` 这条路上）；
  ② 让 coder 为「字段链的基变量」也发一个 ref，使 `VirtualFile:getNode` 能拿到收窄后的 currentValue。
  补充观测（下一轮少走弯路）：`W:traceRef` 里打印 `getValue('t@7:11')` 的结果显示，**读点所在 stack 链上拿到的仍是注解值
  `table | nil`**（不是在分支内被盖掉，而是守卫合并写入的值没在链上），而 `if isarray` 的 else 分支读到的是 `never`
  （`traceIf` 合并时把「某分支没记这个 id」当成 nil 参与 union，`rt.union` 退化）。曾试过在 `traceIf` 合并里用
  「合并前的值」补齐缺失分支 → 这条 probe 仍不复位，且 `test.feature.diagnostic` 多出 1 条 `return-type-mismatch`，已回滚。
  目标工程里这一族：`json-edit.lua` 的 `t.v[k]`（10 处）。
  另：`parser/compile.lua` 的 `child.*`（~20 处）除本族外还叠加了**无 `---@return` 的函数返回值推断成 `nil`**的问题：
  `parseExp()` 的返回值推断出来 view = `nil`（只有 `return nil` 那条被算进去），于是 `if child then` 的收窄结果是 `never`，
  后续 `child.start` / `child.parent = rtn` 全部命中 `mayBeNil` 的注解值。这条要单独查返回值推断（`select` + 多 return）。
- 只在目标工程上下文里出现、单独复刻不复现的候选（本轮试过、无果，需要进程内调试）：
  `main.lua:41`（`if key then _G[key:upper()...]`）、`cpp.lua:247`（`if not transition then break end` 之后）、
  `completion.lua:270`（`lines[firstRow]` 得 `integer | nil`）、`provider/diagnostic.lua:349`（`diag.code`）。
  探针：`tmp/probe-args`、`tmp/probe-break`、`tmp/probe-goto`、`tmp/probe-break2` 均无复现。
- **`need-check-nil` 68 处（目标工程）的分类与已定位根因**：按被报变量的初值形态统计
  （临时在 provider 里打印 `var.loc.id` / 初值 kind / 收窄后 view）：
  `select`（`local a, b = f()` 这类多返回值）**36** / 无初值 **20** / `field`（`x = t.k`）11 / `binary` 1。
  这轮定位到的两条，② 已修（见上），① 仍待做：
  ① **「不返回」的分支没被当成终止**（对应「无初值」那一大桶）：
  ```lua
  local f, err
  if name then f, err = loadfile(name) else f, err = loadfile() end
  if not f then error(err, 2) end
  return f()          -- 这里被报 need-check-nil
  ```
  （目标工程 `tools/lua51.lua:161-167`）。我们只认「以 `return` 结尾的分支」是终止的，
  `error()` 不算，于是 `f` 合并出 `nil`。目标工程里 `if not x then` + 换行 `error(` 的写法有 **12** 处。
  修法：支持 `---@noreturn` —— parser 不用动（未知 subtype 的 cat 只是没有 value）；
  coder 给函数发 `setNoReturn()`（`cat.subtype == 'noreturn'`，参考 `async` 的处理，
  但要放在 `#source.childs > 0` 之外，meta 里的 `function error(message) end` 是空体）；
  `Node.Function` 存标记；tracer 在 `traceCallNarrow` 拿到 callee 后把**当前 stack** 标成 terminated，
  并让 `traceIfChild` 里的 `stack.terminated` 与 AST 判定取或（否则会被覆盖）。
  **坑**：这条要靠 meta 给 `error` 打注解才吃得到目标工程那 12 处，而运行期读的是
  `meta/Lua 5.x auto utf-8/*.lua` 这些**生成产物**（源在 `meta/template/basic.lua`），
  改模板不即时生效（要维护者跑 meta 生成）——所以先在测试里用自定义 `---@noreturn` 函数验证功能。
  ② **`---@type T?` + 多返回值初值的局部，guard 收窄在某个读点没生效**（已修，见上一条「字段读取的
  收窄值要跟基变量的 flow 值走」）：`main.lua:41` 的 `---@type string? local key, tail = v:match '...'`，
  后面 `if key then _G[key:upper():gsub('-', '_')] = getValue(value) end`，被报时打印出来的收窄值仍是
  `string | nil`。（当时排除过的复现路径：换 `---@param v string`、去掉/保留 `ipairs` 循环与分支赋值、
  换普通表达式、去掉 `---@type`——都不复现；根因在字段读取的取值来源，不在这些写法。）
- **`---@cast` 在这个分支里还没有实现**（目标工程 `vm/node.lua:351` 的误报挂在它上面）：
  - `parser/ast/cats/cast.lua` 的 `parseCatStateCastItem` 里 `local exp = self:parseCatExp()` 是**影子变量**，
    外层的 `exp` 永远是 nil，所以 `item.type` 拿不到类型（`---@cast x T` 解析出来没有 `T`）；
  - `vm/coder/cat.lua` 的 `catstatecast` / `catstatecastitem` provider 只编译了类型、**不产生任何收窄**
    （源码里写着 TODO）。
  于是 `---@cast c -vm.global` 这类写法对求值毫无影响，只能靠前后条件的收窄链「碰运气」。
  要做的话：先修上面的影子变量，再给 coder 发一条按位置的收窄指令（`+T` 取交集、`-T` 取 otherSide、
  `?` 并入/去掉 nil，参考 `Narrow:narrowEqual` 的 narrowed/otherSide），tracer 侧对**当时的** flow 值
  应用（不能写死成静态值），最后补 `test/node/tracer.lua` 与 `test/feature/diagnostic/` 用例。
- **`---@cast` 已实现（按位置的收窄）；`vm/node.lua:351` 现在卡在「值被物化成表」**：
  - 已落地：parser 影子变量、coder 的 `compileToPrelude` + `{'cast', ...}` flow 指令、
    tracer 的 `W:traceCast`（断言 / `+T` / `-T` 按同名去掉 / `?`）。
  - 已修：**else 分支里的 `---@cast`** —— `parser/ast/state/if.lua` 的 `parseIfChildElse` 原本
    先 `skipSpace` 再 `blockStart`，`else` 后、首条语句前的注解被算进**外层 block**（编译时 tracer
    节点栈 `nodes=0`，指令落在 if 之外、又被分支收窄覆盖）。改成先 `blockStart` 即可。
    钉住：`test/parser/ast/state.lua`（注解必须挂在 else 分支下）、
    `test/parser/ast/cat.lua`（cast item 的 `type` 不再是 nil）。
  - 仍待做（351 的真正卡点）：else 分支里 `c` 的值是**物化出来的表**（字段里全是 vm.global 的成员），
    不是类/别名联合 —— `removeMemberByName` 取不到名字，回退 `rt.subtract(table, type vm.global)`
    也减不掉（我们的「表 → 类」cast 偏严，见下面「表 cast 偏严」条目）→ `removeObject(c)` 仍报。
    下轮从「分支合并为什么把类联合物化成表」查起（`narrowByField` / `mergeTables` 那条链）。
  - 形参（flow 里没有 `var`）目前只支持不带 op 的断言：试过把变量声明节点也编译到前导区来兜底，
    会 `No such key`（它的值引用在别处、更晚才赋值），已回滚。
- **`and`/`or` 节点的子节点不止两个：操作数自己的 ref 夹在中间**（已修，目标 491 → 470）：
  coder 的 `and`/`or` 节点里，**右操作数**会把自己的 ref 和平铺在同一个节点里
  （`{'or', 左, 右的 ref…, 右}`），而 walker 原本只走 `exp[2]`（左）与 `exp[3]`（右）→
  写在条件里的调用（`if not x or isAlias(x, uri) then`）其参数 ref 落在中间、**永远不被走** →
  参数节点拿不到 `currentValue` → provider 退回注解值 → 报 `Cannot assign string | nil to parameter string`。
  改法（`script/node/tracer.lua` 的 `traceAnd`/`traceOr`）：把 `exp[3..#exp-1]` 的中间 ref 在
  **右操作数自己的 stack**（seed 之后）里 `traceUnit` 走一遍（只读，不做 truthy 收窄）。
  注意**不要**顺手把 `right` 改成 `exp[#exp]`：那样会让 `traceCallTruthy` 作用到真正的调用上，
  目标工程多修 4 处但**新增 16 处**（`workspace.lua` 的 `scp and util.loadFile(...scp.uri...)`
  一族），得不偿失，已回退。
  回归测试：`test/feature/diagnostic/param-type-mismatch.lua`（形状必须带前面那条 `==` 守卫：
  `if childName == 'any' then return true end` 再 `if not childName or isAlias(childName, uri)`；
  只写纯 `not x or f(x)` 不复现）。
  效果：param-type-mismatch −17、need-check-nil −3、return-type-mismatch −1；无新增。
  目标工程命中：`vm/type.lua:466`、`workspace/require-path.lua:190`、`provider.lua:1565` 一族。
  走过的弯路：只在 walker 里给参数节点补 `setCurrentValue` —— 拿不到 id，代码根本走不到，无效。
- **`assign-type-mismatch` 65 处（目标工程）= 表字面量缺「已声明的非可选字段」**：
  `parser/compile.lua:2263` 一族的 `node = getfield` 等，值是分批构造的局部表（`{ dot=..., field=...,
  node=..., type='getfield' }`），而 `parser.object` 声明了一批必填字段。属目标工程注解/写法偏松，
  不是本仓库误报（我们只保留「非可选字段必须齐全」这一条要求），不要为它放宽 cast。
- **`need-check-nil` 里 `compile.lua` 的 `local child = parseExp(); if child then ... child.start` 一族**：
  看着像守卫收窄没生效，但同形状的最小探针（单返回 / 多返回 / `---@type` 三种写法，见 `tmp/q27`）
  都不复现，需要在目标文件上下文里继续缩小（先查 `parseExp` 的返回注解与调用点）。
- **`Table:get` 的 valueMap 分支返回字段记录**（已按「provider 侧展开」修掉，目标 470 → 457，13 处）：
  `valueMap` 存的是 `Node.Field`（字段记录），字面量键读命中它时会**直接返回字段节点**，而其他分支
  返回的是 `field.value`；纯值消费方（读 `xx.field` 的 provider）拿到字段节点就判「字段不存在」
  → 目标工程 `completion.lua:1909`（`func.args`）、`code-action.lua:522`、`duplicate-set-field.lua:88`
  一族（就是 `list[#list+1] = x` 后读 `list[1].field` 的形状）。
  试过直接改 `Table:get` 返回 `field.value`：目标同样到 457，但 `test/feature/definition` 挂 ——
  `t.x` 的**定义**要靠这个字段记录的 location（调用栈：`feature/definition.lua:81` 的
  `findCustomDefinitionAliasFromNode` → `rt:getExpectValue` → `Variable:getExpect` → `Table:get`），
  定义侧与读值侧对 `get` 的期望相反。
  最终做法：在 **`feature/diagnostic/providers/undefined-field.lua`** 里加 `unwrapFieldValue`
  （沿 `field.value` 展开、union 逐成员处理），只影响这一个 provider，0 新增。
  遗留的语义问题：字段读取的 `get` 该返回值还是字段记录要统一定下来（定完 definition 侧改走
  `fieldMap`/`valueMap`，`Table:get` 再统一返回值）。这个形状在测试 harness 里不复现，回归以目标扫描为准。
- **`local child = parseExp(); if child then child.start …` 一族（`need-check-nil`，compile.lua ~5 处）**：
  在目标工程里给 `child@3033:11` 打断点式日志，tracer 走出来的读值是 `never` 和一个 `select`（view `nil`），
  但 provider 仍报 need-check-nil（说明它读到的不是 tracer 的值）。用标注过的返回（`---@return A?`）、
  无注解单返回/多返回（`tmp/q29`、`tmp/q30`）都**不复现** —— 目标工程的 `parseExp` 无返回注解且返回值
  极其庞杂，得在目标文件上下文里继续缩小（下一步：在 need-check-nil provider 里打 `var` 的 alias 与
  `getCurrentValue`/expect/guess 三段来源）。
- **开放表还差「动态/表达式键读取」**：`setDynamic` + provider 出口 + `mergeTables` 传播只覆盖
  「字面量字段读」；`queue[i].x` 这类**表达式键读取**仍被当成 `nil`（`nextTask = queue[i]` 之后
  `nextTask.removed` 报未定义字段、`nextTask.id` 报 need-check-nil）。
  这是我自己的语义结论（开放表 = 任意键都可能存在、值不可判定），不是拿别的实现当判据。
  已试过在 `Table:get` 里给 `self.dynamic` 返回 `ANY, true`（两处：空表提前返回 + 未命中字段），
  结果：① 表达式键读取走的是 `Variable:getChild` 那条路，**根本不经过 `Table:get`**（加打印验证命中 0 次），
  所以对 `queue[i]` 无效；② 反倒把 `---@type string local s = t.anything`（`setmetatable{__index=函数}`）
  这类**故意用 `__index` 函数返回值**的解析压成 ANY，打破 `test/feature/diagnostic/undefined-field.lua`
  里那条「防止退化为 unknown 的假绿」的断言，目标工程还多出 2 条 `any | nil` 的 param-type-mismatch。
  已回滚这两处，只保留 `mergeTables` 传播。下轮要从 `Variable:getChild`/索引读取那条路入手，
  并把 `__index` 的处理排在开放表兜底之前。

- **收窄可以传进闭包了**：外层 block 走到闭包创建点时，把当前收窄快照推给内层 tracer
  （coder 侧在父 flow 插 `seed` 入口 + `setParent`；walker 侧 `getValue` 查不到时查这份快照，
  必要时先触发外层 walk），并让闭包继承外层"已可见变量"集合（upvalue 读取才会挂 tracer）。
  典型修复：`script/config/loader.lua:75` 的 `pcall(function() load(buf, ...) end)`。
  目标工程扫描 792 → 722（param-type-mismatch −18、undefined-field −18、need-check-nil −34）。
- **闭包收窄带来的一族新报**：闭包内读外层变量改成走 flow 后，某些变量的 flow 值
  （分支合并出来的宽 union，常含 `nil`）比原来的注解/赋值推断更宽，于是新报
  （目标工程里 18 条，主要是 `json/jsonc/json-edit` 的 `string_sub(statusBuf, statusPos, ...)`）。
  该族是「闭包收窄可以传进闭包」这次改动引入的（改动前后本仓库自扫对比可见），属我们新增；
  收敛方向：让「外层 flow 值本身带 nil」的变量不挂 tracer（试过两种跳过策略，反而更多，已回滚）。
- **表 cast 偏严（仅剩非可选字段）**：`Table:onCanBeCast` 会遍历期望类型的全部键，
  非可选字段在字面量里缺失时我们报 `assign-type-mismatch`。
  可选字段（`b?`）已按语义放行；剩下的偏严暂不处理，等再遇到同类误报时评估是否放宽
  （放宽需同步更新 `test/node/cast_table.lua` 的严格断言）。
- **`redundant-parameter` 没算末位实参的多返回值展开**：`callArgs = #args`，
  所以 `converter.position(guide.rowColOf(x))`（`rowColOf` 返回 2 值、`position` 收 2 个）
  可能被报「expects a maximum of 1 argument(s)」。`missing-parameter` 已改成按展开计数
  （`#args - 1 + 末位调用的 returns.min`），但 `redundant-parameter` 还没跟；不跟只是漏报（假阴性），
  不会误报，暂不动。
- **`?` 后缀只在部分类型表达式上生效**：目前只有 `catid`（`integer?`）和 `catarray`（`integer[]?`）
  会并入 nil，其余类型表达式仍然丢掉 `?`（探针实测）：`(integer)?` → `integer`、`fun()?` → `fun()`、
  `{ x: integer }?` → `{ x: integer }`、`integer[integer]?` → `nil`（顺带暴露 `catindex` 的值本身也可疑）。
  根因是 `coder/cat.lua` 各 provider 自己拼 `{key} = ...`，没法在 `Coder:compile` 里统一补
  （中间码的 key 只能写一次，二次赋值会报 `Key already exists`）；要修得逐个 provider 处理
  （多语句的 `cattable`/`catfunction` 还得先写临时 key）。目标工程没有用到这些写法，先只修已验证的
  `catarray`，其余等遇到真实误报再说。
- **`redundant-return-value` 取错了返回值**：`local max = funcNode:getReturnCount()` 拿到的是
  `min`（该函数返回 `min, max or nil`）；目标工程现存的 1 处（`script/locale-loader.lua:16`，
  闭包 + `setmetatable` 折叠）还叠加了 `returnsPack` 缓存/空列表的问题，单独改成 `max` 不够，
  需要一起看。
